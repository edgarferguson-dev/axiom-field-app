# Brand assets

The in-app logo is the **Axiom Field** mark from `components/branding/AxiomFieldLogo.tsx` (icon + wordmark).

Optional: drop a raster or alternate vector here (e.g. `axiom-field-mark.svg`) if you later want an `<img>`-based mark instead of the built-in component.
