/** @type {import('next').NextConfig} */
const nextConfig = {
  /**
   * Default dev is webpack (`npm run dev`). If you see `Cannot find module './NNN.js'`
   * in dev, stop the server and run `npm run dev:fresh`. Optional: `npm run dev:turbo`.
   */
};

export default nextConfig;
